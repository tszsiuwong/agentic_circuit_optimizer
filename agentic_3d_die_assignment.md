# Die Assignment 的第一性原理：回归 placement + STA 的所见即所得反馈环

**状态:** 分析文档 v0.1
**读者:** 从事 AI 辅助 3DIC 物理设计的工程师与架构师
**关联文档:**
- `agentic_3d_partition.md`：Agentic 3D Partition 策略生成方案（路线二的工程实现）
- `agentic_tao_physical_design_flow.md`：Agentic TAO 物理设计流程（路线三的框架描述）

---

## 1. 引言：从2D的成功经验说起
- **2D布局的成功秘诀**：其高效收敛的根本原因，在于 `placement` 与 `STA` 构成了一个**紧密的、毫秒级的反馈回路**。布局器每次微调单元坐标，都能通过增量STA即时看到对时序的真实影响，实现“所见即所得”的优化。这是做完placement后时序几乎clean的根源。
- **问题提出**：当设计进入3D细粒度堆叠时代，我们面临一个新变量——**层指派 (die assignment)**。但当前主流方法论却将这个新变量**隔离**在布局和STA的反馈循环之外，试图在布局前“一次性”解决。

## 2. 批判一：独立“分割”步骤违背了“所见即所得”原则
- **核心缺陷**：独立的`partition`步骤（即在布局前固定层指派）完全违背了“所见即所得”原则。
- **决策与反馈脱节**：分割器在**看不到**真实物理距离、绕线拥塞、热分布，尤其是**得不到每步动作级别的增量STA反馈**的情况下，做出层指派这一关键决策。这相当于**在黑暗中瞄准**。
- **乒乓迭代是必然恶果**：由于决策时缺乏关键信息，必须在布局后由STA揭示真实时序，暴露次优决策，然后启动新一轮分割。这种“分割→布局→STA→再分割”的**回合制**迭代，反馈延迟从毫秒级恶化到小时级，是乒乓振荡和收敛缓慢的根源。

## 3. 批判二：“True 3D”布局器仍不够，因为它仍与STA脱节
- **学术进展的局限**：学术界的True 3D布局器虽然将坐标扩展到三维 `(x, y, z)`，但它仍是在**给定的、固定的跨层拓扑**上做优化。它不负责回答“这条路径是否应该被切开”这一结构性问题。
- **仍未进入STA循环**：更重要的是，这类布局器的核心目标仍是**线长最小化**，而非直接由STA驱动的时序优化。它同样没有将`die_assignment`（决定拓扑）与STA时序反馈置于同一优化循环中。
- **联合切分+布局的局限**：`agentic_3d_partition.md` 使用的 DreamPlace 系 True 3D Placer 已做到切分与布局联合执行，突破了“固定的跨层拓扑”这一限定。但即使联合优化，其驱动力仍是线长启发式——判断好坏的依据是一轮完整布局后的聚合 PPA，而非 per-move 的增量 STA 反馈。**反馈延迟从小时级压缩到秒级，但反馈信息的类型并未改变**——仍不是“所见即所得”。

## 4. 核心主张：回归本质，构建 `placement + die_assignment + STA` 三位一体的持续优化闭环
- **核心思想**：`die_assignment` (层指派) 和 `(x, y)` 坐标一样，是决定时序的**根本物理变量**。因此，它必须被纳入 `placement` 与 `STA` 的同一个持续优化循环中。
- **“所见即所得”的3D范式**：
    - **动作**：布局优化器在迭代中，不仅微调单元的 `(x, y)` 坐标，也提出“将这组单元换到另一层”的提案。
    - **即时反馈**：每一次动作（移动或换层）后，都**立即调用增量STA**，精确评估该动作对时序（含跨层路径）的真实影响。
    - **决策**：优化器基于STA的实时反馈，接受、拒绝或进一步调整该动作。
- **理想终局**：在这个持续循环中，`placement` 和 `die_assignment` 的每一步调整都**看着STA的“仪表盘”进行**。直到流片前的最后一刻，只要STA报告还有改善空间，布局器和层指派引擎就会持续协同优化。
- **对 Agent 的意义**：一旦 `placement + die_assignment + STA` 进入毫秒级 per-move 闭环，Agent 的变异-评估周期也从分级压到毫秒级——`die_assignment` 不再是 Agent 外环里的一步离散决策，而是 Agent 每步都可见、可干预的连续物理变量。Agent 不再需要"选一个 partition 策略 → 等内环评估 → 再选下一个"，而是为这个毫秒级闭环设定约束和方向，让它自己跑。这是 Agentic TAO 的终极形态。

## 5. 结论：这是TAO物理设计成功的关键
- 只有将 `die_assignment` 拉回到与 `placement`、`STA` 相同的、持续的、由STA驱动的反馈循环中，才能从根本上解决乒乓迭代的困境。
- 这是让3D IC的物理实现，真正回归到2D设计“所见即所得”这一高效成功范式的**唯一路径**，也是Agentic TAO物理设计流程能否兑现其全部潜能的关键。
- **与路线二的关系**：`agentic_3d_partition.md` 的双环架构把反馈从小时级压缩到分钟级，已是路线二（补丁增强）下能做到的最好程度；本文描述的是路线三的理想形态——把分钟级进一步压缩到毫秒级，最终消灭 partition 这一独立步骤。两篇不互斥，是同一方向上不同粒度的两级。
- **Agent 的角色演进**：在路线二中，Agent 是策略决策者——人在外环定义搜索空间，Agent 在内环通过两层动作空间逐轮试探 tier 归属。在路线三的统一循环中，层指派被下推到布局迭代内部，每步动作直接由增量 STA 驱动——Agent 的角色从"决策者"退化为"约束设定者"：定义 Hard Rules 和 tier 倾向，设定目标函数和收敛边界，然后让循环自己跑。这不是 Agent 的贬值——恰恰相反，当基础循环足够快、反馈足够即时，Agent 才真正从战术操作中解放出来，上升到纯策略层。

---

## 6. 冷静的思考：2D 的闭环是怎么成立的，3D 又凭什么也能成立

本文的核心论证是：2D placement 成功的根源在于 placement 与 STA 构成了毫秒级的"所见即所得"反馈环，而 die assignment 应该被纳入同一个环。这个论证的成立有一个重要的前提——**3D 场景下，这个"所见即所得"的闭环在工程上还能不能真的跑起来？**

这一节正面回答这个问题：先拆开 2D placement 的闭环是怎么实现的（不是 magic，是具体的工程机制），再分析每一条机制在 3D 下是否成立、卡在哪里、需要什么。

### 6.1 2D Placement 的闭环是如何实现的

2D 物理设计中，STA 之所以能成为布局的"即时仪表盘"，靠的不是每次移动都跑一次全芯片 STA，而是基于一套成熟的增量分析方法和巧妙的工程架构。

**增量分析（Incremental Analysis）**

现代布局引擎绝不会在每次单元移动后重新计算全芯片时序。它的工作机制是：

- **维持一个时序图**：工具内部维护一个有向的时序图。每次单元位置变化，只改变图中一小部分边的 RC 延时。
- **"影响锥"（Cone of Influence）**：一次局部的物理改动，其时序影响只会在图中向下游和上游传播到一个有限的范围内。增量 STA 引擎只识别并更新这个受影响锥内的路径，其余部分直接复用上一次的计算结果。
- **代价**：一次增量时序评估的代价，与全芯片 STA 相比可降低几个数量级。

**架构保障：统一数据模型与硬件加速**

- **统一数据模型**：商业引擎的核心数据库将逻辑（网表）、物理（布局/布线）和时序信息统一管理。单元移动能瞬间触发对 RC 寄生参数和时序图的局部更新，无需跨文件格式转换。
- **硬件加速**：学术界已探索用 GPU 加速增量 STA。2025 年的 IncreGPUSTA 在百万门级设计上相比传统 CPU 增量 STA 实现了最高 72.5 倍加速——"毫秒级增量 STA"在 2D 场景下正变得越来越现实。

### 6.2 3D 场景下，闭环为什么面临崩塌风险

2D 的成功经验给 3D 的"所见即所得"提出了极高的技术要求。以下是 2D 和 3D 在物理本质上的差异，逐项对照：

| 对比维度 | 2D Placement 的 STA 反馈 | 3D Die Assignment 的 STA 反馈 |
|:---|:---|:---|
| **物理变化** | 单元在同一平面内小幅移动 | 单元跨层迁移（Tier-to-Tier） |
| **RC 寄生更新** | 局部，影响范围小，增量算法可快速更新 | **全局性**——跨层迁移从根本上改变整条路径的拓扑（层内互连变为键合点互连），影响范围不可简单局部化 |
| **物理效应耦合** | 主要是 RC 和时序 | **热-应力-时序高度耦合**——键合点带来的热应力和温度变化直接改变临近晶体管的延迟，需要全局评估的多物理场问题 |
| **跨角分析** | MMMC 分析成熟 | 需考虑**层间工艺角组合**（如层0慢角、层1快角），组合数爆炸增长 |
| **模型精度** | RC 提取和时序模型经数十年验证，极为成熟 | 3D 互连（混合键合）的 RC 模型和热应力模型仍处于演进中，精度和速度难以兼得 |

核心问题是：2D 增量 STA 依以为基础的"影响范围局部化"前提，在 3D 场景下被打破了——一次跨层迁移改变的不是一条边，而是整条路径的拓扑和与之耦合的物理场。这意味着不能简单地照搬 2D 增量方法，需要在算法层面重新定义"3D 影响锥"。

### 6.3 挑战即是核心研究方向

前述分析并非否定本文的核心论点，而是把问题从"应该怎么做"推到了"怎么做才能成立"的更底层方法论层面。真正的、也是最硬核的研究方向由此浮现：

> **如何构造一个足够准确、且能达到工业实用速度的增量式 3D 时序预言机（Incremental 3D Timing Oracle）？**

这个研究方向本身，就是对"独立分割步骤"和"True 3D 布局"等已有方案的降维打击。它要解决的具体技术难题包括：

1. **3D 专用的增量分析算法**：不能照搬 2D 的"影响锥"概念，需要针对跨层迁移带来的拓扑变化，研究新的跨层路径更新算法，准确界定"3D 影响锥"。
2. **轻量级多物理场代理模型**：放弃签核级精度，为热、应力等效应开发可微分的、快速评估的代理模型（Surrogate Models），让它们在优化循环内"够用就好"。
3. **软硬件协同设计**：利用 GPU 的大规模并行能力，为 3D 场景下复杂的跨层路径更新和热应力计算设计专用的并行数据结构与算法。

### 6.4 重新审视本文的论证

回到本文的核心主张：`placement + die_assignment + STA` 必须三位一体持续优化。§6.2–§6.3 的分析并不削弱这个主张——恰恰相反，它把这个主张从一个"漂亮的想法"升级为一个**可验证的研究命题**。

- 2D placement 的"所见即所得"闭环之所以成立，不是因为物理直觉，而是因为**增量分析 + 统一数据模型 + 硬件加速**三条工程支柱撑起来的。
- 3D 的 die assignment 要进入同一个闭环，需要等价的、但不同于 2D 的三条支柱——它不能直接复用 2D 的增量引擎，但可以**沿相同的工程方法论**重新构造。

**因此，将 die assignment 与 placement、STA 深度绑定，不仅是理论上的最优选择，更是推动 3D EDA 工具从"拼接现有工具"走向"为 3D 重新设计核心引擎"的核心驱动力。** 如果这个增量 3D 时序预言机能够建成，本文描述的闭环就是它的直接应用；如果建不成，那么包括路线二在内的所有替代方案，最好的结果也只是用更多轮次、更大算力去逼近同一个上限——在这个意义上，预言机能否建成，既是 Agentic TAO 物理设计的最大风险，也是其最大机遇。
