# Die Assignment 的第一性原理：回归 placement + STA 的所见即所得反馈环

**状态:** 分析文档 v0.1
**读者:** 从事 AI 辅助 3DIC 物理设计的工程师与架构师
**关联文档:**
- `agentic_3d_partition.md`：Agentic 3D Partition 策略生成方案（路线二的工程实现）
- `agentic_tao_physical_design_flow.md`：Agentic TAO 物理设计流程（路线三的框架描述）

---

## 1. 引言：从2D的成功经验说起
- **2D布局的成功秘诀**：其高效收敛的根本原因，在于 `placement` 与 `STA` 构成了一个**紧密的、毫秒级的反馈回路**。布局器每次微调单元坐标，都能通过增量STA即时看到对时序的真实影响，实现“所见即所得”的优化。这是做完placement后时序几乎clean的根源。
- **问题提出**：当设计进入3D细粒度堆叠时代，我们面临一个新变量——**层指派 (die assignment)**。但当前主流方法论却将这个新变量**隔离**在布局和STA的反馈循环之外，试图在布局前“一次性”解决。

## 2. 批判一：独立“分割”步骤违背了“所见即所得”原则
- **核心缺陷**：独立的`partition`步骤（即在布局前固定层指派）完全违背了“所见即所得”原则。
- **决策与反馈脱节**：分割器在**看不到**真实物理距离、绕线拥塞、热分布，尤其是**得不到每步动作级别的增量STA反馈**的情况下，做出层指派这一关键决策。这相当于**在黑暗中瞄准**。
- **乒乓迭代是必然恶果**：由于决策时缺乏关键信息，必须在布局后由STA揭示真实时序，暴露次优决策，然后启动新一轮分割。这种“分割→布局→STA→再分割”的**回合制**迭代，反馈延迟从毫秒级恶化到小时级，是乒乓振荡和收敛缓慢的根源。

## 3. 批判二：“True 3D”布局器仍不够，因为它仍与STA脱节
- **学术进展的局限**：学术界的True 3D布局器虽然将坐标扩展到三维 `(x, y, z)`，但它仍是在**给定的、固定的跨层拓扑**上做优化。它不负责回答“这条路径是否应该被切开”这一结构性问题。
- **仍未进入STA循环**：更重要的是，这类布局器的核心目标仍是**线长最小化**，而非直接由STA驱动的时序优化。它同样没有将`die_assignment`（决定拓扑）与STA时序反馈置于同一优化循环中。
- **联合切分+布局的局限**：`agentic_3d_partition.md` 使用的 DreamPlace 系 True 3D Placer 已做到切分与布局联合执行，突破了“固定的跨层拓扑”这一限定。但即使联合优化，其驱动力仍是线长启发式——判断好坏的依据是一轮完整布局后的聚合 PPA，而非 per-move 的增量 STA 反馈。**反馈延迟从小时级压缩到秒级，但反馈信息的类型并未改变**——仍不是“所见即所得”。

## 4. 核心主张：回归本质，构建 `placement + die_assignment + STA` 三位一体的持续优化闭环
- **核心思想**：`die_assignment` (层指派) 和 `(x, y)` 坐标一样，是决定时序的**根本物理变量**。因此，它必须被纳入 `placement` 与 `STA` 的同一个持续优化循环中。
- **“所见即所得”的3D范式**：
    - **动作**：布局优化器在迭代中，不仅微调单元的 `(x, y)` 坐标，也提出“将这组单元换到另一层”的提案。
    - **即时反馈**：每一次动作（移动或换层）后，都**立即调用增量STA**，精确评估该动作对时序（含跨层路径）的真实影响。
    - **决策**：优化器基于STA的实时反馈，接受、拒绝或进一步调整该动作。
- **理想终局**：在这个持续循环中，`placement` 和 `die_assignment` 的每一步调整都**看着STA的“仪表盘”进行**。直到流片前的最后一刻，只要STA报告还有改善空间，布局器和层指派引擎就会持续协同优化。
- **对 Agent 的意义**：一旦 `placement + die_assignment + STA` 进入毫秒级 per-move 闭环，Agent 的变异-评估周期也从分级压到毫秒级——`die_assignment` 不再是 Agent 外环里的一步离散决策，而是 Agent 每步都可见、可干预的连续物理变量。Agent 不再需要"选一个 partition 策略 → 等内环评估 → 再选下一个"，而是为这个毫秒级闭环设定约束和方向，让它自己跑。这是 Agentic TAO 的终极形态。

## 5. 结论：这是TAO物理设计成功的关键
- 只有将 `die_assignment` 拉回到与 `placement`、`STA` 相同的、持续的、由STA驱动的反馈循环中，才能从根本上解决乒乓迭代的困境。
- 这是让3D IC的物理实现，真正回归到2D设计“所见即所得”这一高效成功范式的**唯一路径**，也是Agentic TAO物理设计流程能否兑现其全部潜能的关键。
- **与路线二的关系**：`agentic_3d_partition.md` 的双环架构把反馈从小时级压缩到分钟级，已是路线二（补丁增强）下能做到的最好程度；本文描述的是路线三的理想形态——把分钟级进一步压缩到毫秒级，最终消灭 partition 这一独立步骤。两篇不互斥，是同一方向上不同粒度的两级。
- **Agent 的角色演进**：在路线二中，Agent 是策略决策者——人在外环定义搜索空间，Agent 在内环通过两层动作空间逐轮试探 tier 归属。在路线三的统一循环中，层指派被下推到布局迭代内部，每步动作直接由增量 STA 驱动——Agent 的角色从"决策者"退化为"约束设定者"：定义 Hard Rules 和 tier 倾向，设定目标函数和收敛边界，然后让循环自己跑。这不是 Agent 的贬值——恰恰相反，当基础循环足够快、反馈足够即时，Agent 才真正从战术操作中解放出来，上升到纯策略层。

---
