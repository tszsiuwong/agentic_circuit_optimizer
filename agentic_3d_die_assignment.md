# Die Assignment 的第一性原理：回归 placement + STA 的所见即所得反馈环

**状态:** 分析文档 v0.2
**读者:** 从事 AI 辅助 3DIC 物理设计的工程师与架构师
**关联文档:**
- `agentic_3d_partition.md`：Agentic 3D Partition 策略生成方案
- `agentic_tao_physical_design_flow.md`：Agentic TAO 物理设计流程

---

> 本文是方法论层面的论证。如果你来自架构背景，可以这样读：你在 2D 里做架构决策时，STA 可以在毫秒内告诉你影响。在 3D 里，层指派决策的同等反馈不存在——以下是为什么，以及这意味着什么。

## 1. 引言：从 2D 的成功经验说起

- **2D 布局的成功秘诀**：其高效收敛的根本原因，在于 `placement` 与 `STA` 构成了一个**紧密的、毫秒级的反馈回路**。布局器每次微调单元坐标，都能通过增量 STA 即时看到对时序的真实影响，实现"所见即所得"的优化。这是做完 placement 后时序几乎 clean 的根源。
- **问题提出**：当设计进入 3D 细粒度堆叠时代，我们面临一个新变量——**层指派 (die assignment)**。但当前主流方法论却将这个新变量**隔离**在布局和 STA 的反馈循环之外，试图在布局前"一次性"解决。

## 2. 批判一：独立"分割"步骤违背了"所见即所得"原则
- **核心缺陷**：独立的 `partition` 步骤（即在布局前固定层指派）完全违背了"所见即所得"原则。
- **决策与反馈脱节**：分割器在**看不到**真实物理距离、绕线拥塞、热分布，尤其是**得不到每步动作级别的增量 STA 反馈**的情况下，做出层指派这一关键决策。这相当于**在黑暗中瞄准**。
- **乒乓迭代是必然恶果**：由于决策时缺乏关键信息，必须在布局后由 STA 揭示真实时序，暴露次优决策，然后启动新一轮分割。这种"分割→布局→STA→再分割"的**回合制**迭代，反馈延迟从毫秒级恶化到小时级，是乒乓振荡和收敛缓慢的根源。

## 3. 批判二："True 3D"布局器仍不够，因为它仍与 STA 脱节
- **学术进展的局限**：学术界的 True 3D 布局器虽然将坐标扩展到三维 `(x, y, z)`，但它仍是在**给定的、固定的跨层拓扑**上做优化。它不负责回答"这条路径是否应该被切开"这一结构性问题。
- **仍未进入 STA 循环**：更重要的是，这类布局器的核心目标仍是**线长最小化**，而非直接由 STA 驱动的时序优化。它同样没有将 `die_assignment`（决定拓扑）与 STA 时序反馈置于同一优化循环中。
- **联合切分+布局的局限**：`agentic_3d_partition.md` 使用的 DreamPlace 系 True 3D Placer 已做到切分与布局联合执行，突破了"固定的跨层拓扑"这一限定。但即使联合优化，其驱动力仍是线长启发式——判断好坏的依据是一轮完整布局后的聚合 PPA，而非 per-move 的增量 STA 反馈。**反馈延迟从小时级压缩到秒级，但反馈信息的类型并未改变**——仍不是"所见即所得"。

## 4. 核心主张：回归本质，构建 `placement + die_assignment + STA` 三位一体的持续优化闭环
- **核心思想**：`die_assignment` (层指派) 和 `(x, y)` 坐标一样，是决定时序的**根本物理变量**。因此，它必须被纳入 `placement` 与 `STA` 的同一个持续优化循环中。
- **"所见即所得"的 3D 范式**：
    - **动作**：布局优化器在迭代中，不仅微调单元的 `(x, y)` 坐标，也提出"将这组单元换到另一层"的提案。
    - **即时反馈**：每一次动作（移动或换层）后，都**立即调用增量 STA**，精确评估该动作对时序（含跨层路径）的真实影响。
    - **决策**：优化器基于 STA 的实时反馈，接受、拒绝或进一步调整该动作。
- **理想终局**：在这个持续循环中，`placement` 和 `die_assignment` 的每一步调整都**看着 STA 的"仪表盘"进行**。直到流片前的最后一刻，只要 STA 报告还有改善空间，布局器和层指派引擎就会持续协同优化。
- **对 Agent 的意义**：一旦 `placement + die_assignment + STA` 进入毫秒级 per-move 闭环，Agent 的变异-评估周期也从分级压到毫秒级——`die_assignment` 不再是 Agent 外环里的一步离散决策，而是 Agent 每步都可见、可干预的连续物理变量。Agent 不再需要"选一个 partition 策略 → 等内环评估 → 再选下一个"，而是为这个毫秒级闭环设定约束和方向，让它自己跑。这是 Agentic TAO 的终极形态。

## 5. 当前进展：一次低保真验证

在 [logic-folding-placer](https://github.com/tszsiuwong/logic-folding-placer) 的 gcd 实验中，我们在当前工具链的限制下做了一个静态验证：

- 在 placement **完成后**，从 DEF/Verilog 中提取数据，逐网线、逐单元地量化了层指派的物理后果
- 20µm 以上的线在 3D 中 90% 受益（p<0.001），5µm 以下的线 86% 受损
- 层指派与 2D 空间邻近性无关——同/跨 Die 邻居原始距离均为 41µm
- 221 个 HBT 在 1.5µm pitch 下 11% bin 溢流

这仍然是"跑完了回头看"的静态分析——离本节构想的毫秒级闭环还有结构性差距。但它证明了一点：**层指派的物理后果是可量化的，只是当前工具不提供这种反馈。**

## 6. 结论

本文的核心主张是：**`die_assignment` 不是一个可以在布局前独立解决的预处理问题，而是三维布局问题本身的一个固有维度。** 它的优化逻辑与 2D 布局中的 `(x, y)` 坐标完全同构——都必须与 STA 绑定在同一个持续优化的反馈循环中，实现"所见即所得"的时序收敛。

如果这一论证成立，那么它对 `agentic_tao_physical_design_flow.md` 中提出的路线图意味着什么？

- **路线一**（SOTA 工具原样使用）和**路线二**（SOTA 工具 + 补丁增强）的价值不受影响。它们是工程现实下的必要过渡，是验证 TAO 收益、积累经验数据、建立基线的平台。没有路线二的实践经验，我们甚至无法准确描述"联合优化"需要解决什么样的问题。

- **路线三**（一体化新工具）的方向是正确的，但本文试图将它的目标定义得更清晰一些：**"一体化"的核心不是"在同一个数据库中包含 3D 信息"，而是"将层指派作为布局引擎的内生变量，与 STA 在同一个优化循环中持续迭代"。** 这意味着路线三的核心技术挑战是增量式 3D 时序预言机和三维联合优化求解器的构建，而非仅仅扩展数据模型或算法升维。

- **`agentic_tao_physical_design_flow.md` 中描述的"路径感知网表分割器"**，在本文的框架下应被重新理解为：一个在联合优化引擎尚未就绪时**提供初始解和热启动信息**的组件，而非最终架构中的独立优化阶段。当联合优化引擎成熟后，分割器的角色将从"决策者"降级为"建议者"。

本文的贡献在于指出：**3D 物理设计在方法论上可能走了一条弯路——把"层指派"当成分割问题来处理，而它本质上是放置问题的一个新维度。** 如果这个判断是对的，那么 EDA 工具链的长期演进方向，应该从"把分割做得更好"转向"让分割消失在放置之中"。

这是对 `agentic_tao_physical_design_flow.md` 的一个补充视角，而非替代方案。两条路线可以在不同的时间尺度上并行推进，直到工程现实允许我们做出最终选择。
