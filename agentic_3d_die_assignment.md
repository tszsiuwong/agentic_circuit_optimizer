# Die Assignment 的反馈赤字：架构师在黑暗中做层指派决策

**状态:** 分析文档 v0.2
**读者:** 从事 AI 辅助 3DIC 物理设计的工程师与架构师
**关联文档:**
- `agentic_3d_partition.md`：Agentic 3D Partition 策略生成方案
- `agentic_tao_physical_design_flow.md`：Agentic TAO 物理设计流程

---

## 1. 架构师的困境：一个决策，看不到后果

架构师决定一个模块放在哪一层。这个决定影响时序、功耗、键合点密度、热分布——但它是在看不见这些后果的情况下做出的。

2D 布局不存在这个问题。布局器每次微调单元坐标，增量 STA 立刻告诉架构师这一动对时序的影响——毫秒级反馈。这就是为什么 2D 做完 placement 后时序几乎 clean：决策和反馈在同一个高频循环里。

3D 多了一个变量——层指派。但这个新变量不在那个循环里。架构师在布局前做层指派决策，然后等完整的 3D placement 跑完（分钟到小时级），才看到 STA 报告揭示的真实后果。如果后果不理想——重做。决策和反馈之间隔了一整个 P&R 流程。

这不是工具速度的问题。是反馈的结构性缺失：**层指派决策缺少一个毫秒级的"所见即所得"反馈通道。**

## 2. 当前方法的根本缺陷：分割 ≠ 布局

业界主流方法把层指派当成"分割"问题——在布局前，用一个独立的分割器（hMETIS、KaHyPar、或手工标注）把网表切成 2-3 块，每块对应一层，然后各层独立跑 2D P&R。

这个"分割→布局→STA→再分割"的回合制循环有三个致命问题：

**决策与反馈脱节。** 分割器看不到真实物理距离、绕线拥塞、热分布。它在黑暗中瞄准。布局和 STA 跑完后发现分割方案不妥——已经晚了，需要再来一轮。

**反馈延迟从毫秒级恶化到小时级。** 2D placement 的每步微调有即时 STA 反馈。3D 的层指派决策要等整轮 P&R 才有反馈。架构师在 2D 里可以看着 STA 仪表盘做微调，在 3D 里只能"猜→等→看→改→再猜"。

**分割器不知道什么是"好的层指派"。** min-cut 最小化割边数但对时序盲。人工标注依赖经验但不可规模化。即使是 True 3D Placer（切分+布局联合执行），它的驱动力仍是全局线长启发式——判断好坏的依据是一轮完整布局后的聚合 PPA，而非 per-move 的增量 STA。

## 3. "True 3D"布局器仍然不够

学术界的 True 3D 布局器将坐标扩展到三维 (x, y, z)，实现了切分与布局的联合执行。这是重要进步——至少层指派不再是布局前的独立步骤。

但它仍然没有进入 STA 循环。优化目标仍是线长最小化，不是直接由 STA 驱动的时序优化。反馈信息的类型没有改变：判断一个层指派方案好坏的依据，仍然是一轮完整布局后的聚合 PPA，不是 per-move 的增量 STA 反馈。反馈延迟从小时级压缩到秒级，但反馈的**内容**仍是聚合数字，不是每步感知。

## 4. 核心主张：让架构师"看到"层指派的后果

`die_assignment` 和 `(x, y)` 坐标一样，是决定时序的根本物理变量。它必须进入 placement 与 STA 构成的同一个持续优化循环——不是"先分割再布局"，而是"布局过程中持续调整层指派，每步有 STA 反馈"。

这对架构师意味着什么？

- **决策可以被验证。** 架构师决定"模块 A 在 Die0，模块 B 在 Die1"。布局器在优化过程中持续试探这个决定的物理后果——即时 STA 反馈告诉你：这个决定对时序的影响是正还是负、键合点密度是否超限。
- **假设可以被修正。** 如果数据显示 A 和 B 分到不同层导致局部 HBT 溢流，架构师可以在下一轮中调整——不是凭直觉，是对着具体的数字。
- **迭代频率不取决于 P&R 速度。** STA 反馈是毫秒级的，独立于布局器的一次完整收敛。架构师不必等小时级的全流程跑完才能看到后果。

这是 Agentic TAO 的终极形态：`die_assignment` 不再是 Agent 外环里的一步离散决策，而是 Agent 每步都可见、可干预的连续物理变量。Agent 不再需要"选一个 partition 策略 → 等内环评估 → 再选下一个"，而是为毫秒级闭环设定约束和方向，让它自己跑。

## 5. 当前进展

在 [logic-folding-placer](https://github.com/tszsiuwong/logic-folding-placer) 仓库的 gcd 实验中，我们在当前工具链的限制下做了一个低保真验证：

- 在 placement **完成后**，从 DEF/Verilog 中提取数据，逐网线、逐单元地量化层指派的物理后果
- 发现 20µm 以上的线在 3D 中 90% 受益（p<0.001），5µm 以下的线 86% 受损
- 层指派与 2D 空间邻近性无关——同/跨 Die 邻居的原始距离均为 41µm
- 221 个 HBT 在 1.5µm pitch 下 11% bin 溢流

这仍然是"跑完了回头看"的静态分析，离本文构想的毫秒级闭环还有结构性差距。但它证明了：**层指派的物理后果是可量化的，架构师需要的反馈是可以通过数据获得的。**

## 6. 结论

本文的核心主张：`die_assignment` 不是一个可以在布局前解决的预处理问题，而是布局问题本身的固有维度。它的优化逻辑与 2D 布局中的 (x, y) 坐标同构——都必须与 STA 绑定在同一个持续优化的反馈循环中。

对路线图的影响：

- **路线一和路线二的价值不受影响。** 它们是验证 TAO 收益、积累经验数据的必要过渡。没有路线二的实践经验，甚至无法准确描述"联合优化"需要解决什么样的问题。
- **路线三的目标**："一体化"的核心不是"在同一个数据库里包含 3D 信息"，而是"将层指派作为布局引擎的内生变量，与 STA 在同一个优化循环中持续迭代"。
- **路径感知网表分割器**在联合优化引擎尚未就绪时，是提供初始解和热启动信息的组件。当联合优化引擎成熟后，它的角色将从"决策者"降级为"建议者"。

本文的贡献在于指出：3D 物理设计把"层指派"当成分割问题来处理，而它的本质是放置问题的一个新维度。架构师目前在黑暗中做层指派决策——因为反馈不在循环里。把反馈放回去，是路线三的核心命题。
