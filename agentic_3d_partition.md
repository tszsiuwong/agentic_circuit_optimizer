# Agentic TAO 物理设计流程：物理设计工程师视角

**状态:** 分析文档 v0.1

**读者:** 从事 AI 辅助(agentic)物理设计、3D 集成与 EDA 工具开发的工程师、架构师与研究者

---
## 1. 已有的基础梳理：3DIC 物理设计引擎

### 1.1 与北大林亦波老师 DreamPlace 团队合作积累

- **GPU 加速 True 3D Placer**：输入 2D 网表，同时执行切分与布局，输出 3D 网表方案
- **GPU 加速 Pseudo 3D Placer**：输入 2D 网表 + Tier Assignment，快速布局评估（38-82s，支撑千万级 instance）
- **HeteroPlace + HeteroSTA**：GPU 加速布局与时序分析，秒级 PPA 反馈
- **热/力学基础仿真**：热阻提取与 warpage 分析能力

### 1.2 开源 OpenROAD + Pin3D 体系

作为学术探索的另一条腿，开源体系提供了完全可控的实验环境和算法原型：

- **Yosys + ABC**：开源逻辑综合与工艺映射，可从 RTL 直达门级网表，支持自定义工艺库
- **Pin3D**：基于 OpenROAD 的伪 3D 布局流程，通过 Shrunk-2D 或 Compact-2D 策略将 3D 布局转化为平面布局器可处理的形式，是学术 3D 物理设计的主流验证平台

### 1.3 商用 3D EDA 工具现状

当前商用 3D-IC 平台本质上均为 2D APR 的扩展：

- **3D Global Placement**：拆分后线长/时序驱动上下 Die + Hybrid Bonding 联合布局
- **Pseudo 3D Flow**：Cascade-2D 变种，anchor cell → Zero Delay Cell
- **ILM 3D Flow**：接近 Pin3D 思路，double Metal Stack PDK → 两侧分别抽 ILM SPEF 接口模式

两条腿互补：商业 EDA 保证签核质量，开源体系支撑算法快速迭代和学术可复现性。

---

## 2. Agentic Partition 策略生成构想

核心定位：**不做新的布局器，做 partition 的决策层**——用 Agent 自动生成 partition 策略，用已有引擎做快速评估，形成闭环。

### 2.1 现有方案的问题

当前 3DIC partition 的方案有三类，各有致命缺陷：

| 方案 | 做法 | 致命问题 |
|------|------|---------|
| **人工 partition** | 工程师凭经验选层次、分模块 | 耗时数天，经验不可复用，试错成本极高 |
| **min-cut 分割**（hMETIS/KaHyPar） | 超图最小割边数 | 对时序完全盲，无键合点资源约束 |
| **EFS + RL** | Cadence 启发式 Floorplan 引擎出 Partition 方案，布局工具评估，挂 RL 自动搜索 | 每轮数小时，数百轮才收敛；基于 module 粒度，不感知网表内部逻辑 |

三种方案的共同缺陷：**决策和评估在同一个慢速循环里**——每次调整 partition 都要跑完整 P&R 才知道效果。

### 2.2 核心思路：双环解耦

决策与评估分离：

```
┌─────────────────────────────────────────┐
│           Master Agent                   │
│   策略生成（网表层次/模块归属/路径分配）    │
│   Hard Rules（工艺边界）                  │
└──────────────────┬──────────────────────┘
                   │
    ┌──────────────▼──────────────┐
    │   内环（Fast Loop）          │
    │   ⏩ 分钟级探索               │
    │                              │
    │   3D DreamPlace GPU 布局      │
    │   + HeteroSTA 时序分析        │
    │   + Pseudo 3D Placer         │
    │   RL/BO/Pareto 搜索          │
    │   → 百轮探索 → Top-K 候选     │
    └──────────────┬──────────────┘
                   │ Top-K
    ┌──────────────▼──────────────┐
    │   外环（Slow Loop）          │
    │   ⏸ 小时级验证               │
    │                              │
    │   商业 EDA 全流程 P&R         │
    │   → signoff 级 PPA 验证       │
    └──────────────┬──────────────┘
                   │ 精确 PPA
    ┌──────────────▼──────────────┐
    │   闭环学习                    │
    │   策略知识库沉淀              │
    │   代理模型持续校准            │
    └──────────────────────────────┘
```

- **内环**：用已有 GPU 加速引擎（Pseudo 3D Placer + HeteroSTA）做秒级评估，Agent 在分钟级内探索上百种 partition 策略
- **外环**：商业 EDA 全流程验证 Top-K 候选方案，小时级产出 signoff 精度 PPA
- **闭环**：外环结果反馈校准内环代理模型，Agent 策略偏好持续优化

### 2.3 但入口仍然是网表——前端信息的缺失是根因

上述双环架构解决了"评估慢"的问题，但没有解决"信息盲"的问题。引擎的入口是**门级网表**：里面只有单元、连线、时序弧——没有模块功能语义、没有数据流意图、没有架构师的分层直觉。

当前 partition 探索的实际工作模式：

```
门级网表 → 跑一版 3D 布局 → 看时序报告 → 画 Pareto 前沿 → 猜哪个模块该切 → 再跑
```

本质上是**纯后端盲迭代**。时序报告告诉你"这条路径慢了"，但它不会告诉你这条路径是 decode 逻辑还是 execute 逻辑，是控制流还是数据流。在 Pareto 前沿上选点，本质上是在缺少结构信息的情况下做黑盒试探。双环可以让你更快地完成这个试探——但方向仍然是猜的。

而 TAO 体系的核心洞见恰恰在于：**partition 策略的质量上限由前端信息决定**。知道"这条路径是译码逻辑、那条是执行流水线"和"这是一条关键路径"之间的差距，就是盲迭代和有方向搜索之间的差距。

如果能把前端文档 §4.2 的变异目录和 TAO 文档 §2.4 的三态 tier 标注引入，partition 的入口就从"门级网表"扩展到"携带架构意图的门级网表"——这会把盲迭代变成结构化搜索。这恰好是 §3.5 要讨论的策略产生机制的前提。


### 2.4 清醒认知：开源与商用的 Gap 必须正视

作为后端出身的工程师，必须诚实地说：OpenROAD/Pin3D 等开源体系与商业 EDA 之间，存在**不可忽视的差距**——不是"性能差一点"的问题。这个 Gap 在 2D 场景下就已经非常显著，3DIC 叠加了键合点合法化、跨层 CTS、多层热分析等额外维度后只会更大：

- **3D 布局质量**：开源引擎的线长/密度/拥塞优化与商用引擎在千万级 instance 设计上仍有显著差距，学术 benchmark 上的 good result 不等于工业设计上的可用结果
- **签核精度**：寄生提取、STA、DRC/LVS 的签核质量差距是数量级的——商业工具的签核流程经过了数十年工艺迭代验证，开源生态短期内无法追赶
- **3D 特有功能缺失**：键合点合法化、跨层 CTS、多层热分析在开源体系里几乎空白，而这些恰恰是 3DIC partition 评估的核心维度
- **鲁棒性与稳定性**：商业工具在 corner case、大规模设计、多工艺角下的稳定性经过了大量工业设计验证，开源引擎的边界条件远未充分探索

**这个 Gap 不会自动消失，必须被解决。** 写这份方案如果不正视这一点，就是在回避最困难的问题。解决路径不是"等开源变好"，而是：

1. **外环永远用商业 EDA**：签核和最终 PPA 判定不走开源，确保结论可信
2. **内环代理模型必须持续校准**：开源快速评估器（Pseudo 3D Placer + HeteroSTA）的偏差，每一轮外环商业 EDA 结果都要用来修正
3. **用 Agent 的闭环能力弥补单点精度**：单次开源评估可能偏差 15%，但一百轮有方向的结构化迭代 + 商业 EDA 逐轮校准，可以在 PPA 上逼近甚至超越单次人工最优——Agent 闭环的价值正在于此

这不是"开源替代商业"的叙事，而是**开源承担探索、商业负责判决的分层架构**。承认 Gap 才能管理 Gap。

---

## 3. 与《Agentic TAO Physical Design Flow》的对齐

### 3.1 双环架构

前端文档 §6.3 的"代理模型预筛 + 真实签核校准"与上一章的设计完全同构。"内环/外环/校准管道"三层抽象，恰好为这个方案提供了系统化的描述语言。

### 3.2 分割作为一等优化变量

文章的核心判断（§2.3）——细粒度逻辑折叠的收益关键不在布局器而在分割器——和工程直觉高度一致。同一个设计、不同的人切，结果天差地别。min-cut 方案不够用的根源正是"时序盲"，而文章提出的"关键度加权路径折叠"给出了明确的形式化方向。

### 3.3 源码标注的三态契约

文章的 free/hint/locked 三态设计（§2.4），对定义"Agent ↔ EDA 工具"的接口极有参考价值。工艺硬约束（热失配、噪声耦合、不可切模块、键合点密度上限）天然映射为 locked 态——工业侧独有的输入，学术 benchmark 里没有。

### 3.4 工具路线的选择

短期定位接近于文章路线二（补丁增强）：保留商业 EDA 做外环签核，合作开发分割器与快速评估器构成内环。做通用一体化 3D EDA 引擎（路线三）超出了当前能力边界，但长期来看，若内环的代理模型精度瓶颈需要更深的工具协同来突破，路线三是一个值得关注的演进方向。

---

### 3.5 策略从哪来：从变异目录到 Agentic 闭环

前端文档描述的 Agentic 自优化循环（§4.1-4.3）给出了策略产生的标准范式：**Agent 不是凭空发明策略，而是从结构化的变异目录中提取假设，逐条验证，形成闭环**。这个范式可以平行映射到 partition 域：

```
┌─────────────────────────────────────────────┐
│         Partition 策略目录（变异空间）        │
│                                              │
│  ① 网表层次选择   ② 模块归属决策              │
│  ③ 关键路径同 Die  ④ Control/Compute 拆分     │
│  ⑤ 键合点密度约束  ⑥ 热平衡约束               │
│  ⑦ 架构意图对齐（tier 标注，TAO §2.4）        │
│  ...                                         │
│  Hard Rules（locked 态：工艺边界，不可变异）    │
└──────────────────┬──────────────────────────┘
                   │ 每一轮：提取一个假设
                   ▼
┌──────────────────────────────────────────────┐
│  ① 提案：Master Agent 从目录中选取策略         │
│     "把关键路径 X 的模块强制放在同一 Die"       │
│     "将网表层次从 module 级提升到 partition 级" │
└──────────────────┬───────────────────────────┘
                   ▼
┌──────────────────────────────────────────────┐
│  ② 等价门：策略是否违反 Hard Rules？           │
│     不通过 → 拒绝，写决策记录                   │
└──────────────────┬───────────────────────────┘
                   ▼ 通过
┌──────────────────────────────────────────────┐
│  ③ 测量：内环 Pseudo 3D Placer + HeteroSTA    │
│     秒级 PPA 反馈（HPWL / 面积 / 键合点数）     │
└──────────────────┬───────────────────────────┘
                   ▼
┌──────────────────────────────────────────────┐
│  ④ 决策：接受 / 回退 / 分支                    │
│     Top-K 进入外环商业 EDA 验证                 │
│     外环结果 → 校准内环代理模型 → 回写策略目录   │
│     正负结论都写决策记录                         │
└──────────────────────────────────────────────┘
```

这个闭环和前端文档 §4.1 完全同构，只替换了三个插槽：

| 前端环 | 后端 Partition 环 |
|--------|------------------|
| 变异目录：流水线切分、旁路、投机、资源复制… | 变异目录：层次选择、模块归属、Control/Compute 拆分… |
| 等价门：提交点轨迹等价 + LEC | 等价门：Hard Rules 工艺边界检查 |
| 传感器：CPI 归因直方图 + 综合 STA | 传感器：Pseudo 3D Placer + HeteroSTA + 热代理模型 |

策略不是一次性"生成"的产出物，而是**在闭环中持续进化**的知识库——每条策略携带"假设 → 验证结果 → 适用范围"的完整记录，这是和 min-cut 等一次性算法的根本区别。

---

## 4. 已有基础对齐

| 已有基础 | 在本方案中的角色 |
|---------|-------------|
| **Pseudo 3D Placer** | 内环核心评估器——已交付、已验证 |
| **HeteroPlace + HeteroSTA** | 内环秒级 PPA 反馈 |
| **HeteroAgent**（2026.05 发布） | Master Agent 骨架 |
| **True 3D Placer** | Partition 策略探索辅助验证 |
| **热/力学仿真** | 内环热代理模型 |

---

## 5. 本方案在 TAO 全流程中的位置

以下将本方案映射到《Agentic TAO Physical Design Flow》描述的完整物理设计链上。标注 **★** 的步骤是本方案直接覆盖或强依赖的环节。

```
Verilog RTL
      │
      ▼
① 逻辑综合（分割友好化）                        ◀── 已有：Yosys + ABC 开源综合
      │                                               未来可探索综合时的 tier 感知优化
      ▼
② 路径感知网表分割  ★ 本方案核心                    ◀── 已有：True 3D Placer（切分+布局联合）
      │                                               Agent 注入 partition 策略
      │                                               Hard Rules 守卫工艺边界
      │                                               输出：每单元 tier 映射 + 跨层网清单
      ▼
③ 层间对齐布图规划                                  ◀── 已有：商业 EDA 全流程
      │                                               未来：热/力学仿真反馈指导 Macro 层指派
      ▼
④ 3D 布局  ★ 本方案内环引擎                         ◀── 已有：Pseudo 3D Placer（38-82s 快速评估）
      │                                               GPU 加速 DreamPlace 系解析布局
      │                                               覆盖 LoL / LoM / 上下 Die 错位 / 热驱动
      ▼
⑤ 层间键合点分配                                    ◀── 已有：商业 EDA 处理
      │                                               未来：键合点密度纳入 Agent 约束
      ▼
⑥ 跨层时钟树综合                                    ◀── 已有：商业 EDA 处理
      │
      ▼
⑦ 分层布线                                          ◀── 已有：商业 EDA 处理
      │
      ▼
⑧ 3D 寄生提取与统一 STA  ★ 本方案内环传感器           ◀── 已有：HeteroSTA（GPU 加速秒级时序）
      │                                               未来：跨层统一时序图（当前用预算法分割）
      ▼
⑨ 热与供电完整性  ★ 本方案内环传感器                  ◀── 已有：热阻提取 + warpage 仿真
      │                                               目标：增量热估计模型进入内环
      ▼
⑩ 签核（DRC/LVS/最终 STA） ★ 本方案外环               ◀── 商业 EDA 全流程
      │                                               Agent 不参与签核，只接收结果反馈
      ▼
⑪ 逻辑等价检查（LEC）                                ◀── 商业 EDA
                                                         Agent 变异不改变逻辑，LEC 恒过

              闭环反馈 ──→ ② 更新 partition 策略 ──→ 知识库沉淀
```

### 在 TAO 体系中的定位总结

| TAO 流程步骤 | 本方案覆盖 | 形态 |
|-------------|----------|------|
| ② 路径感知分割 | **核心** | Agent 策略生成 + True 3D Placer 辅助验证 |
| ④ 3D 布局 | **内环引擎** | Pseudo 3D Placer 快速评估 |
| ⑧ 统一 STA | **内环传感器** | HeteroSTA 秒级时序 |
| ⑨ 热/IR | **内环传感器** | 热代理模型（目标：进入内环快评） |
| ⑩ 签核 | **外环** | 商业 EDA，Agent 只读结果 |
| ①③⑤⑥⑦⑪ | 不覆盖 | 由商业 EDA 或开源工具承担 |

本方案的价值在于填补了 TAO 流程中 **"② 路径感知分割"这一目前缺失的决策层**——不是重新实现布局/布线/时序/热分析，而是用 Agentic 方法解决"这些成熟的执行引擎应该往哪个方向跑"的问题。

---

## 6. 几个开放问题

1. **分割粒度**：单元级折叠 vs 模块级 partition，在工程上最优颗粒度应该在哪里？是否存在一个"可控性好 + 收益充分"的 sweet spot？

2. **乒乓收敛**：分割-布局乒乓（文章 §4.2）可能振荡。如果 locked 态约束足够多，是否可以保证收敛？或者是否有绕过乒乓的方案（如把层指派做成布局器中的连续变量）？

3. **源码标注的实践**：三态 tier 标注从 PD 侧看非常精巧，但标注信息穿透 PyCircuit → RTL → 综合 → 网表，在工程上最大的挑战是哪一步？